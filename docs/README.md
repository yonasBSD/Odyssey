# Odyssey Docs

Local development:

```bash
cd docs
npm install
npm start
```

Production build with the GitHub Pages base path:

```bash
cd docs
npm run build
```

Released Rust API docs live on docs.rs.
